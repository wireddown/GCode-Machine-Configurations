;;
;;     Choose one!
;;       the only "M505" that's uncommented wins ;P
;;

; M505 P"opt"        ; Use the active profile    -- /sys/opt/
; M505 P"prod"     ; Use the prev profile -- /sys/prod/
M505 P"zerocal" ; Use the experimental profile -- /sys/zerocal/

M98 P"config.g"    ; Use that folder's configuration
