;;
;;     Choose one!
;;       the only "M505" that's uncommented wins ;P
;;

M505 P"opt"        ; Use the in-work profile    -- /sys/opt/
; M505 P"prod"     ; Use the production profile -- /sys/prod/

M98 P"config.g"    ; Use that folder's configuration
