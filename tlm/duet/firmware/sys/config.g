;;
;;     Choose one!
;;       the only "M505" that's uncommented wins ;P
;;

; M505 P"opt"         ; Use /sys/opt/config.g
; M505 P"prod"        ; Use /sys/prod/config.g
; M505 P"zerocal"     ; Use /sys/zerocal/config.g

M505 P"smrteff"
M98  P"config.g"    ; Use that folder's configuration
